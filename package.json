{
  "name": "memothon",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "$npm_execpath run build && vite preview",
    "deploy": "$npm_execpath run build && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:generate": "drizzle-kit generate",
    "db:push": "dotenvx run --env-file=.dev.vars -- drizzle-kit push",
    "auth:generate": "dotenvx run --env-file=.dev.vars -- bunx cli generate --config src/auth.ts --output src/db/auth-schema.ts -y",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "better-auth": "^1.3.27",
    "drizzle-orm": "^0.44.6",
    "hono": "^4.9.11"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.3.27",
    "@cloudflare/vite-plugin": "^1.2.3",
    "@dotenvx/dotenvx": "^1.51.0",
    "@vitest/ui": "^4.0.6",
    "drizzle-kit": "^0.31.5",
    "happy-dom": "^20.0.10",
    "vite": "^6.3.5",
    "vite-ssr-components": "^0.5.1",
    "vitest": "^4.0.6",
    "wrangler": "^4.17.0"
  }
}